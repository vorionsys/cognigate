fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
structlog>=24.1.0
httpx>=0.26.0

# Auth / JWT
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.0

# Cryptography
cryptography>=42.0.0

# Policy Engine
pyyaml>=6.0.1
jsonschema>=4.21.0

# Cache (optional - graceful degradation)
redis>=5.0.0

# AI Critic Providers — lazy-imported, install only the ones you use.
# Uncomment for local development or non-Vercel deploys:
# anthropic>=0.18.0
# openai>=1.12.0
# google-generativeai>=0.4.0

# Testing (dev only)
# pip install -e ".[dev]"  — see pyproject.toml for dev dependencies
